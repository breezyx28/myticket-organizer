# Copy to `.env` and adjust. See comments in `.env`.

# Organizer API (import.meta.env.VITE_API_BASE_URL) — src/config/api.ts
VITE_API_BASE_URL=http://localhost:8000

# Optional: main site for forgot/reset password handoff — src/config/site.ts
# VITE_MAIN_SITE_URL=https://myticket.com
